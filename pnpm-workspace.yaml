# Horca dependency domain root.
# Deliberately lists only the root: Horca's own dependencies are independent of
# the pinned Orca dependency graph, which is installed inside the materialized
# upstream worktree from Orca's own lockfile.
packages:
  - '.'
